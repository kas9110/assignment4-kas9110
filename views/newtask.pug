h2 Upload a task 
//create a post form that asks for the task, description, and creates a button
.row
    .col-md-6.col-md-offset-3
        form(method = 'POST' action="/users")
            div.form-group
                label(for='task') Task Name: 
                input#task.form-control(type='text', placeholder = 'task', name='task')
            div.form-group
                label(for='description') Description: 
                input#description.form-control(type='text', placeholder = 'description', name='description')
            button.btn.btn-primary(type='submit') Upload your task